# Supabase project settings → Project Settings → API
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Supabase project settings → Database → Connection string → Transaction pooler (Supavisor).
# Pooled, not direct — see docs/spec.md §12 (connection exhaustion is the
# most common way this stack falls over).
DATABASE_URL=

# OpenRouter (v2 extraction pipeline, §9)
OPENROUTER_API_KEY=

# Resend (v2 mail, §12)
RESEND_API_KEY=
